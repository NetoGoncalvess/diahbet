# URL de conexão do PostgreSQL (Neon, Render, etc)
DATABASE_URL=postgresql://usuario:senha@host/nome_do_banco?sslmode=require

# Porta (o Render define automaticamente em produção, so precisa localmente)
PORT=3000
